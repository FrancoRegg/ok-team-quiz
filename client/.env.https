# Variables para `npm run dev:https` (modo "https" de Vite).
# Vacías a propósito: tienen prioridad sobre .env.local, así el socket y la API
# usan el mismo origen HTTPS y viajan por el proxy de Vite hasta el backend.
VITE_SOCKET_URL=
VITE_API_URL=
